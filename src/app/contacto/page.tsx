import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contacto",
};

// TODO: Formulario de contacto y datos de la empresa (teléfono, email,
// dirección en Cunit). El envío del formulario debe procesarse server-side.
export default function ContactoPage() {
    return <main>Contenido pendiente: formulario e información de contacto.</main>;
}
