import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Zona de cobertura: El Vendrell",
};

// TODO: Landing geográfica general (silo /zonas/) para El Vendrell.
// Enlazar con /pladur/el-vendrell/ y /conductos-de-aire/el-vendrell/.
export default function ZonaElVendrellPage() {
    return <main>Contenido pendiente: zona de cobertura El Vendrell.</main>;
}
