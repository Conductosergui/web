import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Zonas de cobertura",
};

// TODO: Listado de zonas de cobertura (silo /zonas/). areaServed en un radio de 50 km
// (Baix Penedès, Garraf, Alt Penedès, Tarragonès, Baix Llobregat).
// Sub-rutas: /zonas/vilanova-i-la-geltru/, /zonas/el-vendrell/, /zonas/sitges/
export default function ZonasPage() {
    return <main>Contenido pendiente: listado de zonas de cobertura.</main>;
}
